services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./prompts:/app/prompts
      - ./app:/app/app
    restart: always
