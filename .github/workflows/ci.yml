name: TechGear Chatbot CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      # Optional: if you need a separate db service, but here we use the containerized app
      # which includes ChromaDB. We'll build the whole stack.

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Create .env file
      run: |
        echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> .env

    - name: Build and Start Container
      run: |
        docker compose up -d --build

    - name: Wait for server to be ready
      run: |
        timeout 60s bash -c 'until curl -s http://localhost:8000/api/health; do sleep 2; done'

    - name: Run Integration Tests
      run: |
        export PYTHONPATH=$PYTHONPATH:.
        pytest app/test_runner.py -v
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

    - name: Stop and cleanup
      if: always()
      run: docker compose down
